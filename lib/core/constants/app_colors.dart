import 'package:flutter/material.dart';

class AppColors {
  // Light Mode Colors
  static const Color primaryLight = Color(0xFF3F51B5); // Indigo Blue
  static const Color accentLight = Color(0xFF00BCD4); // Vibrant Teal
  static const Color backgroundLight = Color(
    0xFFF5F5F5,
  ); // Light mode background
  static const Color primaryTextLight = Color(
    0xFF212121,
  ); // Primary Text (Dark Gray)
  static const Color secondaryTextLight = Color(
    0xFF757575,
  ); // Secondary Text (Gray)

  // Dark Mode Colors
  static const Color primaryDark = Color(0xFF7986CB); // Indigo Blue (Dark Mode)
  static const Color accentDark = Color(0xFF00BCD4); // Vibrant Teal
  static const Color backgroundDark = Color(0xFF121212); // Dark mode background
  static const Color primaryTextDark = Color(
    0xFFE0E0E0,
  ); // Primary Text (Light Gray)
  static const Color secondaryTextDark = Color(
    0xFFBDBDBD,
  ); // Secondary Text (Light Gray)
}
